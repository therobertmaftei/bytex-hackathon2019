# Ngrx Arch

## Dev Setup

### Start Dev Server

Run `npm start` and open the browser on `http://localhost:4200`

### Run the build

Run `npm run build:dev`

## Production Setup

### Run the build

Run `npm run build`
