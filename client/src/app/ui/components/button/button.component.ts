import { Component } from '@angular/core';
import { ElementCore } from '../core';

@Component({
  selector: 'cd-button',
  templateUrl: './button.component.html',
  styleUrls: ['./button.component.scss']
})
export class ButtonComponent extends ElementCore { }
